# qckfx Homebrew Tap

Homebrew tap for [qckfx](https://qckfx.com) - screenshot-based iOS testing tool.

## Installation

```bash
brew tap qckfx/tap
brew install --cask qckfx
```

Or as a one-liner:

```bash
brew install --cask qckfx/tap/qckfx
```
